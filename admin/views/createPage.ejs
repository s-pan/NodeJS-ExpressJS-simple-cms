<html>
  <head>
    <%- include head.ejs %>
    <%- include summernote.ejs %>
    <script src="/admin/bundle.js"></script>
  </head>
  <body>
      <%- include header.ejs%>
       <main id="main" class="container">
          <form action="/admin/add-page" method="post" id="my-form">
            <div class="form-group">
               <label for="title">Title</label>
               <input type="text" id='title' name="title" class="form-control">
            </div>
             <div class="form-group">
               <label for="meta-keywords"> Meta Keywords</label>
               <input type="text" name="keywords" id="meta-keywords" class="form-control">
            </div> 
             <div class="form-group">
               <label for="meta-description"> Meta Description</label>
                <input type="text" name="description" id="meta-description" class="form-control">
            </div>
             <div class="form-group">
                <label for="title"> Category</label>
                <select name="category">
                  <option value='primary'>Primary</option>
                   <% if (categories) { %>
                     <% categories.forEach(function(category){ %>
                      <option value=<%= category.title %>><%= category.title %></option>
                    <% }) %>
                  <% } %>
                </select>
            </div>          
          <input type="file" name="image">
          <textarea id="summernote" name="textarea"></textarea>
          <input type="submit" value="Submit" class="btn btn-lg btn-danger">
         </form>

         
<!-- Button trigger modal -->
<button type="button" class="btn btn-primary btn-lg" data-toggle="modal" data-target="#myModal">
  Launch demo modal
</button>

<!-- Modal -->
<div id="file-manager" class="container">
 
</div>
<script>
$('#ajax-btn').click(function(e){
  $.ajax({
  method: "POST",
  url: "/t",
}).done(function(msg){
  $('form').html(msg)
})
})

$('.selected-image').click(function(event){
  console.log(event)
  console.log(222222222222)
})

</script>
       <main>

       <% if(typeof locals.img != 'undefined') { %>
          <img src=<%= img %>>
       <% } %>
       
       </main>
<script>
$(document).ready(function() {
  $('#summernote').summernote();
  $('#summernote').summernote('insertImage', '/admin/upload');
})
</script>
     



    </body>
</html>